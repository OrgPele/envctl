# Envctl Commit Log

### Envctl pointer ###
Assert headless tmux plan launches stay detached

- Scope: harden headless tmux plan-launch behavior for both fresh launches and already-running tmux clients, and document the exact detached behavior with startup-flow regressions.
- Behavior changes: verifies `--plan --tmux --opencode --headless` launches tmux with `new-session -d`, never executes `attach`, `attach-session`, or `switch-client`, fails the test if an interactive child attach is attempted, and now resolves existing-session attach targets with the correct manual follow-up command (`attach-session` outside tmux, `switch-client` inside tmux).
- Files touched: `python/envctl_engine/planning/plan_agent_launch_support.py`, `tests/python/startup/test_startup_orchestrator_flow.py`, and this commit ledger.
- Tests run: `python3 -m unittest tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_real_tmux_launch_stays_detached_and_prints_attach_instructions` ✅; `python3 -m unittest tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_inside_tmux_does_not_switch_client_and_prints_manual_attach_command` ✅; `python3 -m unittest tests.python.startup.test_startup_orchestrator_flow tests.python.planning.test_plan_agent_launch_support tests.python.runtime.test_cli_router tests.python.runtime.test_cli_router_parity` ✅ (122 tests).
- Build/config/env: no repo config, migrations, or environment bootstrap changes.
- Risks/notes: coverage remains mocked at the tmux process boundary, but it now proves both fresh-launch and inside-tmux headless paths stay detached while the manual attach hint matches the caller's tmux context.
