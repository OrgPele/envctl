# Envctl Commit Log

### Envctl pointer ###
Assert headless tmux plan launches stay detached

- Scope: add lower-level regression coverage for existing-session tmux reuse when the caller is already inside tmux.
- Behavior changes: verifies existing-session plan-agent tmux reuse returns a manual `switch-client` attach target from inside tmux, complementing the existing startup-flow coverage that `--plan --tmux --opencode --headless` creates fresh tmux sessions with `new-session -d`, never executes `attach`, `attach-session`, or `switch-client`, and fails if an interactive child attach is attempted.
- Files touched: `tests/python/planning/test_plan_agent_launch_support.py` and this commit ledger.
- Tests run: `PYTHONPATH=python python3 -m unittest tests.python.planning.test_plan_agent_launch_support.PlanAgentLaunchSupportTests.test_tmux_launch_reuses_existing_session_with_switch_client_inside_tmux tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_inside_tmux_does_not_switch_client_and_prints_manual_attach_command` ✅; `PYTHONPATH=python python3 -m unittest tests.python.startup.test_startup_orchestrator_flow tests.python.planning.test_plan_agent_launch_support tests.python.runtime.test_cli_router tests.python.runtime.test_cli_router_parity` ✅ (122 tests); LSP diagnostics on changed Python files ✅.
- Build/config/env: no repo config, migrations, or environment bootstrap changes.
- Risks/notes: coverage remains mocked at the tmux process boundary, but it now proves the existing-session attach target matches the caller's tmux context while the already-present startup-flow tests prove headless paths stay detached.
