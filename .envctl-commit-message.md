fix(migrate): compact summaries and keep dashboard colors

- Scope:
  - reduce terminal spam for multi-target migrate failures in both direct CLI and dashboard-interactive flows
  - keep the dashboard visual language for migrate results by preserving green/red symbols and per-project color rotation
  - preserve persisted migrate metadata and report artifacts unchanged

- Key behavior changes:
  - `python/envctl_engine/actions/action_command_orchestrator.py`
    - keeps the shared compact multi-failure summary printer for migrate
    - suppresses `backend env source` hint lines from visible terminal summaries while leaving them in persisted metadata
    - when multiple targets fail, prints per-target headlines, at most one shared actionable hint, and one compact `migrate failure logs:` section instead of one full log block per target
    - applies dashboard-style color treatment to migrate summaries: green `✓`, red `✗`, and rotating colored project names in route order
  - `tests/python/actions/test_actions_parity.py`
    - adds coverage that direct CLI mixed-result and compact multi-failure summaries keep the expected colorized symbols and project-name styling
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
    - adds dashboard-side regression coverage for the same compact multi-failure summary shape and color styling
    - updates existing interactive migrate assertions to validate visible content via `strip_ansi(...)`
  - `tests/python/runtime/test_prompt_install_support.py`
    - updates the prompt-template expectation to the current `envctl commit --headless --main` guidance so the full suite stays green after upstream prompt changes

- Files/modules touched:
  - `python/envctl_engine/actions/action_command_orchestrator.py`
  - `tests/python/actions/test_actions_parity.py`
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
  - `tests/python/runtime/test_prompt_install_support.py`
  - `.envctl-commit-message.md`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity.ActionsParityTests.test_direct_cli_migrate_mixed_results_prints_successes_and_failures_in_route_order` -> pass
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity.ActionsParityTests.test_direct_cli_migrate_multiple_failures_compact_shared_hints_and_logs` -> pass
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector.DashboardOrchestratorRestartSelectorTests.test_project_action_failure_details_compact_multi_failure_logs_and_hints` -> pass
  - `python3 -m unittest tests.python.runtime.test_prompt_install_support.PromptInstallSupportTests.test_renderers_produce_expected_target_shapes` -> pass
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector tests.python.actions.test_actions_parity` -> pass (`Ran 183 tests`)
  - `envctl test --project Main` -> pass (`1671 passed, 0 failed, 0 skipped`, duration `2m 2s`)

- Config/env/migrations:
  - no config schema changes
  - no migrations
  - no report artifact format changes; only terminal rendering and prompt-test expectations changed

- Risks/notes:
  - multi-target failure summaries intentionally omit terminal printing of the persisted `backend env source` hint line to keep the output scannable
  - single-target failure summaries still print the fuller per-target hint/detail block
