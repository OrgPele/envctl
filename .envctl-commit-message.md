Consolidate runtime dependency resolution helpers

Scope:
- Route startup resume/restore dependency access through the shared runtime-context resolver helpers.
- Route lifecycle cleanup dependency access through the same shared runtime-context resolver helpers while preserving
  compatibility wrapper methods.
- Replace the worktree action runner's private process-runner protocol with the shared process runtime protocol.
- Add structure guards that prevent local state-repository, port-allocator, and process-runtime protocol clones from
  returning in these owner modules.

Key behavior changes:
- No intended CLI, startup restore, lifecycle cleanup, worktree deletion, state persistence, or process execution
  behavior changed.
- Runtime collaborators still prefer `runtime.runtime_context` and fall back to legacy runtime attributes where hidden
  or compatibility callers may still patch them.

Files/modules touched:
- `python/envctl_engine/startup/resume_restore_policy.py`
- `python/envctl_engine/runtime/lifecycle_cleanup_orchestrator.py`
- `python/envctl_engine/actions/actions_worktree.py`
- `tests/python/shared/test_structure_layout.py`

Tests run:
- `uv run --extra dev pytest -q tests/python/shared/test_structure_layout.py -q` -> failed first for the expected local
  protocol clones and missing shared helper imports.
- `uv run --extra dev pytest -q tests/python/shared/test_structure_layout.py tests/python/runtime/test_runtime_context_protocols.py tests/python/startup/test_resume_restore_policy.py tests/python/runtime/test_lifecycle_cleanup_spinner_integration.py tests/python/actions/test_action_worktree_runner.py` -> 119 passed, 132 subtests passed.
- `uv run --extra dev ruff check python/envctl_engine/startup/resume_restore_policy.py python/envctl_engine/runtime/lifecycle_cleanup_orchestrator.py python/envctl_engine/actions/actions_worktree.py tests/python/shared/test_structure_layout.py` -> passed.
- `uv run --extra dev python -m py_compile python/envctl_engine/startup/resume_restore_policy.py python/envctl_engine/runtime/lifecycle_cleanup_orchestrator.py python/envctl_engine/actions/actions_worktree.py` -> passed.
- `git diff --check` -> passed.

Config/env/migrations:
- None.

Risks/notes:
- This is a dependency-resolution cleanup only; compatibility accessors remain in place for existing tests and hidden
  callers.
