# Envctl Commit Log

### Envctl pointer ###
Keep headless codex tmux launches detached from the caller terminal

- Scope: teach the `codex-tmux` utility to honor the repo-wide headless/batch flag and add focused regression coverage for detached tmux launches.
- Behavior changes: when `codex-tmux` is invoked with `--headless`/`--batch`, envctl still creates or reuses the repo-scoped tmux session but no longer executes `attach-session` or `switch-client`; it prints a manual `attach:` command instead.
- Files touched: `python/envctl_engine/runtime/codex_tmux_support.py`, `tests/python/runtime/test_codex_tmux_support.py`, and this commit ledger.
- Tests run: `PYTHONPATH=python python3 -m unittest tests.python.runtime.test_codex_tmux_support` ✅ (6 tests); `PYTHONPATH=python python3 -m unittest tests.python.runtime.test_cli_router tests.python.runtime.test_cli_router_parity tests.python.runtime.test_engine_runtime_dispatch` ✅ (34 tests); `PYTHONPATH=python python3 -m unittest tests.python.startup.test_startup_orchestrator_flow` ✅ (13 tests).
- Build/config/env: no config, migration, or environment contract changes. `python3 -m ruff check ...` and `python3 -m basedpyright ...` were attempted for the changed files but those modules are not installed in this environment.
- Risks/notes: headless `codex-tmux` now mirrors the detached behavior already covered for plan-agent tmux launches, but the tmux process boundary remains test-double based rather than exercising a real tmux server.
