### Envctl pointer ###
Archive completed dead-code cleanup task

Scope:
- Archive the completed prior MAIN_TASK.md as OLD_TASK_1.md.
- Replace MAIN_TASK.md with a completion-audit task that explicitly states no implementation work remains.

Behavior changes:
- No runtime or product behavior changes.
- The new MAIN_TASK.md records evidence that the stale-module cleanup was fully implemented in commit 5e8a306 and PR #227.
- The new MAIN_TASK.md preserves the acceptance criteria for keeping deleted stale modules absent and retained string-invoked entry points present.

Files/modules touched:
- OLD_TASK_1.md
- MAIN_TASK.md
- .envctl-commit-message.md

Tests run:
- PYTHONPATH=python python -m unittest tests.python.shared.test_structure_layout: passed
- envctl test --project Main: passed, 2352 passed, 16 skipped

Config/env/migrations:
- No config, environment, persisted state, or migration changes.

Risks/notes:
- This is task bookkeeping for the next implementation iteration; no code implementation changes are required because the audit found the prior scope complete.
- `envctl test --project 3` and `envctl test --project refactoring_envctl_dead_code_cleanup-3` did not match registered targets; `envctl --list-trees --json` reported no tree projects, so final envctl validation used the registered `Main` project target.
