# Envctl Commit Log

### Envctl pointer ###
Assert headless tmux plan launches stay detached

- Scope: add startup-flow regression coverage for headless tmux plan launches, focused on the integrated fresh tmux launch path.
- Behavior changes: verifies `--plan --tmux --opencode --headless` creates tmux with `new-session -d`, never records `attach`, `attach-session`, or `switch-client`, and fails the test if the process runner attempts an interactive child attach; printed attach/kill instructions remain available for manual follow-up.
- Files touched: `tests/python/startup/test_startup_orchestrator_flow.py` and this commit ledger.
- Tests run: `python3 -m unittest tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_real_tmux_launch_stays_detached_and_prints_attach_instructions` ✅; `python3 -m unittest tests.python.startup.test_startup_orchestrator_flow tests.python.planning.test_plan_agent_launch_support tests.python.runtime.test_cli_router tests.python.runtime.test_cli_router_parity` ✅ (120 tests).
- Build/config/env: no repo config, migrations, or runtime behavior changes.
- Risks/notes: coverage remains mocked at the tmux process boundary, but it now proves both the integrated fresh-launch path and existing-session reuse path stay detached in headless startup flow.
