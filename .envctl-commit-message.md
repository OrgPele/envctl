fix(migrate): compact multi-target failure summaries

- Scope:
  - reduce terminal spam for multi-target migrate failures in both direct CLI and dashboard-interactive flows
  - keep per-target failure headlines while collapsing repeated hints and repeated log-path chrome
  - preserve persisted migrate metadata and report artifacts unchanged

- Key behavior changes:
  - `python/envctl_engine/actions/action_command_orchestrator.py`
    - adds a shared migrate result printer used by the direct CLI path
    - suppresses `backend env source` hint lines from visible terminal summaries while leaving them in persisted metadata
    - when multiple targets fail, prints per-target headlines, at most one shared actionable hint, and one compact `migrate failure logs:` section instead of one full log block per target
    - collapses shared report directories so multi-target output shows the directory once and then lists target -> log filename entries
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
    - reuses the same shared migrate result printer so dashboard-interactive output matches the direct CLI compaction rules
  - `tests/python/actions/test_actions_parity.py`
    - adds coverage for direct CLI multi-failure rendering, including shared-hint compaction and grouped log output
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
    - adds the dashboard-side regression for the same compact multi-failure summary shape

- Files/modules touched:
  - `python/envctl_engine/actions/action_command_orchestrator.py`
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
  - `tests/python/actions/test_actions_parity.py`
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
  - `.envctl-commit-message.md`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity.ActionsParityTests.test_direct_cli_migrate_multiple_failures_compact_shared_hints_and_logs` -> pass
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector.DashboardOrchestratorRestartSelectorTests.test_project_action_failure_details_compact_multi_failure_logs_and_hints` -> pass
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_target_support` -> pass (`Ran 8 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_spinner_integration` -> pass (`Ran 5 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity` -> pass (`Ran 119 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector` -> pass (`Ran 64 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_terminal_ui_dashboard_loop` -> pass (`Ran 16 tests`)
  - `envctl test --project Main` -> pass (`1671 passed, 0 failed, 0 skipped`, duration `2m 4s`)

- Config/env/migrations:
  - no config schema changes
  - no migrations
  - no report artifact format changes; only terminal rendering changed

- Risks/notes:
  - multi-target failure summaries now intentionally omit terminal printing of the persisted `backend env source` hint line to keep the output scannable
  - single-target failure summaries still print the fuller per-target hint/detail block
