docs(env): document frontend override resolution

- Scope:
  - document the shared env-file override contract for frontend service startup alongside the existing backend/migrate contract
  - add frontend-focused regression coverage for relative override resolution, ambiguity failure, and fallback to `frontend/.env`
  - complete manual multi-worktree verification for startup migrations, native `migrate`, shell-env scrubbing, and repo-relative frontend overrides

- Key behavior changes:
  - `docs/reference/configuration.md`
    - added a dedicated `Frontend Service Env Resolution` section describing `FRONTEND_ENV_FILE_OVERRIDE` and `MAIN_FRONTEND_ENV_FILE_PATH`
    - documented the shared absolute / target-root / repo-root / ambiguity / default-fallback contract for frontend env files
  - `docs/operations/troubleshooting.md`
    - added operator guidance for diagnosing frontend env-file selection during service startup
    - clarified that frontend startup shares the override-path contract with backend startup/bootstrap, while backend-only migrate diagnostics stay backend-scoped
  - `tests/python/startup/test_service_bootstrap_domain.py`
    - added frontend resolver coverage for ambiguous relative overrides and missing-override fallback to `frontend/.env`
  - `tests/python/runtime/test_engine_runtime_real_startup.py`
    - added runtime coverage proving repo-relative `FRONTEND_ENV_FILE_OVERRIDE` works for trees targets
    - added runtime coverage proving repo-relative `MAIN_FRONTEND_ENV_FILE_PATH` works in Main mode

- Files/modules touched:
  - `docs/reference/configuration.md`
  - `docs/operations/troubleshooting.md`
  - `tests/python/startup/test_service_bootstrap_domain.py`
  - `tests/python/runtime/test_engine_runtime_real_startup.py`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.startup.test_service_bootstrap_domain` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.runtime.test_engine_runtime_real_startup` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity` -> passed

- Manual verification:
  - `PYTHONPATH=python <runner-venv>/bin/python -m envctl_engine.runtime.cli --repo <in-repo scratch fixture> --trees --project feature-a-1 --project feature-b-1 --batch` with `FRONTEND_ENV_FILE_OVERRIDE=config/frontend.shared.env` and conflicting shell `DATABASE_URL` / `APP_ENV_FILE` exported -> startup migrations used each target's own `backend/.env`, and frontend startup loaded the repo-relative override for both targets
  - `PYTHONPATH=python <runner-venv>/bin/python -m envctl_engine.runtime.cli --repo <in-repo scratch fixture> migrate --project feature-a-1 --batch` with conflicting shell backend vars exported -> passed and used `feature-a-1/backend/.env`
  - `PYTHONPATH=python <runner-venv>/bin/python -m envctl_engine.runtime.cli --repo <in-repo scratch fixture> migrate --project feature-b-1 --batch` with conflicting shell backend vars exported -> passed and used `feature-b-1/backend/.env`
  - `PYTHONPATH=python <runner-venv>/bin/python -m envctl_engine.runtime.cli --repo <in-repo scratch fixture> stop-all --batch` -> passed cleanup

- Config/env/migrations:
  - no config key changes
  - no migration revision changes
  - manual verification used a temporary in-repo fixture with `ENVCTL_BACKEND_MIGRATIONS_ON_STARTUP=true`, default backend `.env` files per worktree, and a repo-relative `FRONTEND_ENV_FILE_OVERRIDE`

- Risks/notes:
  - manual verification exercised the real CLI against a faithful fixture repo rather than a production app, so backend env ownership was verified through observed subprocess env values instead of real Alembic revisions
  - no runtime code changes were needed because the added frontend regression tests confirmed the shipped contract already matched the docs
