docs(tasking): carry forward env verification gaps

- Scope:
  - archive the completed backend-isolation implementation task
  - rewrite `MAIN_TASK.md` so it contains only the verified remaining scope
  - retarget the next iteration around frontend override docs and manual multi-worktree verification closure

- Key behavior changes:
  - `OLD_TASK_2.md`
    - preserves the prior broad backend env-isolation implementation plan unchanged for audit history
  - `MAIN_TASK.md`
    - now focuses only on remaining gaps confirmed by repo evidence
    - requires authoritative docs for `FRONTEND_ENV_FILE_OVERRIDE` and `MAIN_FRONTEND_ENV_FILE_PATH`
    - requires explicit manual verification of backend env isolation and frontend override resolution
    - makes clear that any runtime mismatch found during verification must be fixed in the same iteration

- Files/modules touched:
  - `OLD_TASK_2.md`
  - `MAIN_TASK.md`
  - `.envctl-commit-message.md`

- Tests run:
  - not run; this pass only audited git/code/docs state and rewrote task-planning artifacts

- Config/env/migrations:
  - no config key changes
  - no migration or runtime-code changes
  - new task explicitly carries forward required verification of `ENVCTL_BACKEND_MIGRATIONS_ON_STARTUP`, backend override vars, and frontend override vars

- Risks/notes:
  - the new task assumes the landed backend runtime/test changes are complete unless manual verification proves otherwise
  - manual verification evidence still lives outside the repo until the next implementation pass records it in the PR/final response
