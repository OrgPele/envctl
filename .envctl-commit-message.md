# Envctl Commit Log

### Envctl pointer ###
Assert headless tmux plan launches stay detached

- Scope: add startup-flow regression coverage for headless tmux plan launches, including reuse of an already-existing envctl tmux session for the matching worktree.
- Behavior changes: verifies `--plan --tmux --opencode --headless` keeps the current terminal detached both when envctl creates a fresh tmux session with `new-session -d` and when launch support returns an existing-session attach target; both paths print attach/kill instructions instead of calling `attach_plan_agent_terminal`.
- Files touched: `tests/python/startup/test_startup_orchestrator_flow.py` and this commit ledger.
- Tests run: `.venv/bin/python -m unittest tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_real_tmux_launch_stays_detached_and_prints_attach_instructions` ✅; `.venv/bin/python -m unittest tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_with_existing_tmux_session_prints_attach_instructions_without_attaching tests.python.startup.test_startup_orchestrator_flow.StartupOrchestratorFlowTests.test_headless_plan_real_tmux_launch_stays_detached_and_prints_attach_instructions` ✅; `.venv/bin/python -m unittest tests.python.startup.test_startup_orchestrator_flow tests.python.planning.test_plan_agent_launch_support tests.python.runtime.test_cli_router tests.python.runtime.test_cli_router_parity` ✅ (120 tests).
- Build/config/env: no repo config, migrations, or runtime behavior changes.
- Risks/notes: coverage remains mocked at the tmux process boundary, but it now proves both the integrated fresh-launch path and existing-session reuse path stay detached in headless startup flow.
