fix(migrate): align direct cli and dashboard result summaries

- Scope:
  - finish the remaining direct non-interactive `migrate` summary work from `MAIN_TASK.md`
  - suppress the legacy raw non-interactive migrate success/failure lines and print one compact post-run per-target summary instead
  - harden dashboard-interactive migrate summary rendering for `m --all` / state-derived project selections
  - record the required real-TTY/manual verification commands and observed outcomes

- Key behavior changes:
  - `python/envctl_engine/actions/action_target_support.py`
    - adds `print_noninteractive_failures` and `print_noninteractive_successes` controls so migrate can suppress the old `migrate action succeeded/failed ...` lines without affecting other actions
  - `python/envctl_engine/actions/action_command_orchestrator.py`
    - adds `_MigrateResultRecord` normalization and shared migrate result-summary helpers
    - defers direct CLI migrate summary printing until after the action spinner/status lifecycle completes
    - uses persisted `project_action_reports` metadata first and falls back to in-memory observed outcomes when persisted entries are missing
    - prints success and failure lines in route order, dedupes hint lines, and prints exactly one failure-log block per failed target
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
    - reuses the shared migrate record helper so dashboard and direct CLI rendering stay aligned
    - fixes dashboard-interactive migrate summaries for `--all` / state-derived `SimpleProject` selections by normalizing project objects to real project names before looking up persisted reports
  - `tests/python/actions/test_actions_parity.py`
    - adds direct CLI migrate coverage for compact failure output, mixed success/failure ordering, and all-success result visibility
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
    - adds regression coverage for dashboard-interactive migrate summaries when the route relies on state-derived projects instead of explicit `route.projects`

- Files/modules touched:
  - `python/envctl_engine/actions/action_target_support.py`
  - `python/envctl_engine/actions/action_command_orchestrator.py`
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
  - `tests/python/actions/test_actions_parity.py`
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
  - `MAIN_TASK.md`
  - `.envctl-commit-message.md`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity` -> pass (`Ran 118 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_target_support` -> pass (`Ran 8 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_spinner_integration` -> pass (`Ran 5 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_command_orchestrator_targets` -> pass (`Ran 8 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector` -> pass (`Ran 63 tests`)
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_terminal_ui_dashboard_loop` -> pass (`Ran 16 tests`)
  - focused TDD check:
    - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector.DashboardOrchestratorRestartSelectorTests.test_project_action_failure_details_print_migrate_results_for_all_selection_using_state_projects` -> failed before fix, passed after fix

- Manual verification:
  - scratch fixture seeded inside this worktree only:
    - repo root: `/Users/kfiramar/projects/current/envctl/trees/broken_envctl_migrate_output_1dedup_success_visibility_and_spinner_parity/1/.manual_verify_migrate_cli/repo`
    - runtime root: `/Users/kfiramar/projects/current/envctl/trees/broken_envctl_migrate_output_1dedup_success_visibility_and_spinner_parity/1/.manual_verify_migrate_cli/runtime`
  - direct CLI real-TTY command:
    - `env RUN_REPO_ROOT="$PWD/.manual_verify_migrate_cli/repo" RUN_SH_RUNTIME_DIR="$PWD/.manual_verify_migrate_cli/runtime" ENVCTL_DEFAULT_MODE=trees ENVCTL_UI_HYPERLINK_MODE=off PYTHONPATH="$PWD/python" python3 -m envctl_engine.runtime.cli migrate --all`
    - observed:
      - printed `✓ migrate succeeded for feature-a-1`
      - printed `✗ migrate failed for feature-b-1: pydantic_core._pydantic_core.ValidationError: 2 validation errors for Settings`
      - printed bounded hint lines and exactly one `migrate failure log for feature-b-1:` block
      - did not print the raw traceback wall in terminal output
  - dashboard-interactive real-TTY command:
    - launch: `env RUN_REPO_ROOT="$PWD/.manual_verify_migrate_cli/repo" RUN_SH_RUNTIME_DIR="$PWD/.manual_verify_migrate_cli/runtime" ENVCTL_DEFAULT_MODE=trees ENVCTL_UI_HYPERLINK_MODE=off PYTHONPATH="$PWD/python" python3 -m envctl_engine.runtime.cli dashboard`
    - interactive input: `m --all`
    - observed after fix:
      - printed both success and failure result lines for the selected targets
      - failure output led with the actionable `ValidationError` headline, not `Traceback`
      - printed one failure-log block for `feature-b-1`
      - returned to the dashboard prompt after the migrate run
  - state inspection command:
    - `env RUN_REPO_ROOT="$PWD/.manual_verify_migrate_cli/repo" RUN_SH_RUNTIME_DIR="$PWD/.manual_verify_migrate_cli/runtime" ENVCTL_DEFAULT_MODE=trees PYTHONPATH="$PWD/python" python3 -m envctl_engine.runtime.cli show-state --json`
    - observed:
      - `project_action_reports.feature-b-1.migrate.report_path` persisted
      - `project_action_reports.feature-b-1.migrate.backend_env.env_file_path` and `env_file_source=default` persisted
      - `project_action_reports.feature-b-1.migrate.headline` persisted as the additive failure headline
  - persisted raw failure artifact check:
    - `.manual_verify_migrate_cli/runtime/python-engine/repo-ddab33104ee0/runs/manual-migrate-run/feature-b-1_migrate.txt`
    - observed raw `Traceback (most recent call last): ...` payload still present only in the report file
  - spinner/status evidence:
    - the local Python environment used for manual verification reported `ui.spinner.disabled` with `reason=spinner_backend_missing`, so an animated rich spinner could not be observed in this shell
    - the same runs emitted ordered per-target `ui.status` updates (`Running migrate for 2 targets...`, `Running migrate for feature-a-1 (1/2)...`, `Running migrate for feature-b-1 (2/2)...`, final success/failure status lines)
    - dashboard-interactive runs also emitted `ui.spinner.disabled` with `reason=interactive_command_spinner_suppressed`, preserving one visible owner on that path
    - automated spinner coverage remains green in `tests.python.actions.test_action_spinner_integration`

- Config/env/migrations:
  - no config schema changes
  - no persistent env contract changes beyond migrate result-report metadata already expected by the task
  - no database/schema migrations

- Risks/notes:
  - manual verification used an in-worktree scratch repo and a fake local `alembic.py` module to exercise the real CLI paths deterministically without touching sibling worktrees or external repos
  - the manual shell environment lacked the rich spinner backend, so the verification evidence for live per-target progress is the real-TTY transcript plus emitted `ui.status` events rather than an animated spinner capture
