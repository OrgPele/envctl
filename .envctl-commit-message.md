fix(runtime): harden final validation packaging flow

- Scope:
  - archive the completed Python runtime gap-closure task state in `MAIN_TASK.md`
  - keep the refreshed completion-only task narrative for this worktree
  - fix the final validation regression where `envctl test` polluted packaged CLI smoke tests with inherited repo-launcher env
  - make runtime CLI repo selection honor explicit `--repo` over inherited `RUN_REPO_ROOT`

- Key behavior changes:
  - `python/envctl_engine/runtime/cli.py`
    - resolves `--repo` before inherited `RUN_REPO_ROOT`
    - writes the resolved repo back into `RUN_REPO_ROOT` before route parsing and config loading so explicit repo targeting stays authoritative end-to-end
  - `tests/python/runtime/test_command_exit_codes.py`
    - adds coverage for explicit `--repo` overriding inherited `RUN_REPO_ROOT`
  - `tests/python/runtime/test_cli_packaging.py`
    - adds a regression test for installed-package behavior under inherited source-checkout env
    - scrubs launcher-private and interpreter-coupled env vars from the installed-package smoke-test helper so packaged installs are validated in isolation
  - `MAIN_TASK.md`
    - remains a completion-confirmation task stating no Python runtime gap-closure implementation work remains

- Files/modules touched:
  - `MAIN_TASK.md`
  - `.envctl-commit-message.md`
  - `python/envctl_engine/runtime/cli.py`
  - `tests/python/runtime/test_command_exit_codes.py`
  - `tests/python/runtime/test_cli_packaging.py`

- Tests run:
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes.CommandExitCodeTests.test_explicit_repo_overrides_inherited_run_repo_root` -> passed
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes.CommandExitCodeTests.test_doctor_repo_resolves_root_without_bootstrap` -> passed
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes.CommandExitCodeTests.test_start_command_fails_with_installed_command_repair_guidance` -> passed
  - `python3 -m unittest tests.python.runtime.test_cli_packaging.CliPackagingTests.test_regular_install_without_dependencies_fails_before_operational_dispatch` -> passed
  - `python3 -m unittest tests.python.runtime.test_cli_packaging.CliPackagingTests.test_regular_install_without_dependencies_ignores_inherited_source_checkout_env` -> passed
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes tests.python.runtime.test_cli_packaging` -> passed (`61` tests)
  - `./bin/envctl list-targets --json` -> passed (`Main` target resolved for the current worktree root)
  - `./bin/envctl test --project Main` -> passed (`1660` passed, `0` failed)

- Config/env/migrations:
  - no config schema changes
  - no migrations
  - test helper now strips inherited launcher env (`ENVCTL_ROOT_DIR`, `RUN_REPO_ROOT`, `RUN_LAUNCHER_*`, `RUN_ENGINE_PATH`, `PYTHONPATH`, `VIRTUAL_ENV`) before installed-package smoke validation

- Risks/notes:
  - packaged CLI smoke tests intentionally validate in an isolated env now; if future tests need launcher-context inheritance they should opt in explicitly instead of relying on ambient parent-process state
  - this commit keeps the completion-only `MAIN_TASK.md` intact rather than reopening the finished runtime-gap-closure scope
