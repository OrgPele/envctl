merge(main): reconcile runtime validation and migrate updates

- Scope:
  - merge the latest `origin/main` into `refactoring_python_runtime_gap_closure-1`
  - preserve the runtime validation and packaged-cli regression fixes from this branch
  - keep the newer migrate summary, dashboard color, and prompt-template updates from `main`

- Key behavior changes:
  - `python/envctl_engine/runtime/cli.py`
    - keeps explicit `--repo` authoritative over inherited `RUN_REPO_ROOT`
    - preserves mainline repo-root bootstrap behavior for non-`--repo` invocations
  - `tests/python/runtime/test_command_exit_codes.py`
    - retains coverage proving explicit `--repo` overrides inherited `RUN_REPO_ROOT`
  - `tests/python/runtime/test_cli_packaging.py`
    - keeps the installed-package repo-targeting regression coverage from `main`
    - keeps the inherited source-checkout env regression coverage from this branch
    - uses the shared `_isolated_runtime_env()` helper from `main` to scrub launcher/runtime env before installed-package smoke tests
  - `MAIN_TASK.md`
    - remains the completion-confirmation task for the runtime gap-closure worktree instead of adopting unrelated task text from `main`

- Files/modules touched:
  - `.envctl-commit-message.md`
  - `MAIN_TASK.md`
  - `python/envctl_engine/runtime/cli.py`
  - `tests/python/runtime/test_cli_packaging.py`
  - merge-integrated upstream files from `origin/main`

- Tests run:
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes tests.python.runtime.test_cli_packaging`
  - `./bin/envctl test --project Main`

- Config/env/migrations:
  - no config schema changes
  - no migrations
  - installed-package smoke helpers continue to isolate launcher/runtime env (`ENVCTL_ROOT_DIR`, `RUN_REPO_ROOT`, `RUN_LAUNCHER_*`, `RUN_ENGINE_PATH`, `PYTHONPATH`, `VIRTUAL_ENV`, related envctl runtime flags)

- Risks/notes:
  - `MAIN_TASK.md` intentionally stays branch-specific because it is the authoritative task file for this worktree
  - merge resolution preserves both upstream migrate-output changes and this branch’s runtime validation regression fixes
