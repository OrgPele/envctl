Fix dashboard command input ownership guard

Scope:
- Extend the import-ownership audit so the new dashboard command-input owner is an approved location for
  interactive command parsing helper definitions.
- Keep the `command_support.dispatch_kill_session` compatibility wrapper callable without an explicit
  selector function while still delegating to `command_input_support`.
- Add focused coverage for the compatibility wrapper default path.

Key behavior changes:
- No dashboard user-facing behavior changes are intended.
- Hidden callers that still invoke `command_support.dispatch_kill_session(runtime)` remain supported after the
  command-input extraction.
- Full-suite ownership guards now recognize `ui/dashboard/command_input_support.py` as a valid helper owner.

Files/modules touched:
- `python/envctl_engine/ui/dashboard/command_support.py`
- `tests/python/shared/test_import_audit.py`
- `tests/python/ui/test_dashboard_command_support.py`

Tests run:
- `uv run --extra dev pytest -q tests/python/shared/test_import_audit.py tests/python/ui/test_dashboard_command_support.py tests/python/shared/test_structure_layout.py` -> 91 passed, 124 subtests passed.
- `uv run --extra dev ruff check python/envctl_engine/ui/dashboard/command_support.py tests/python/ui/test_dashboard_command_support.py tests/python/shared/test_import_audit.py` -> passed.
- `uv run --extra dev pytest -q tests/python/ui/test_dashboard_*.py tests/python/shared/test_import_audit.py tests/python/shared/test_structure_layout.py` -> 294 passed, 132 subtests passed.
- `git diff --check` -> passed.

Config/env/migrations:
- None.

Risks/notes:
- Serena diagnostics still report the pre-existing `Route` import-symbol warning in `command_support.py`; the
  focused runtime and structure tests pass with the existing import contract.
