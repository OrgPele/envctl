fix(migrate): dedupe migrate output and surface per-target results

- Scope:
  - make migrate failure headlines concise and actionable instead of leading with traceback chrome
  - preserve migrate success visibility in dashboard-interactive mode for mixed-result and all-success multi-target runs
  - give direct CLI action spinners live per-target migrate progress through the existing `ui.status` event stream

- Key behavior changes:
  - `python/envctl_engine/actions/action_target_support.py`
    - added an optional targeted-action failure status formatter so migrate can emit bounded single-line failure statuses while preserving raw failure payloads for persistence hooks
  - `python/envctl_engine/actions/action_command_orchestrator.py`
    - added an action-level spinner bridge that updates the active action spinner from later `ui.status` events when the action spinner is the visible owner
    - added migrate headline extraction/reordering so persisted summaries and new additive `headline` metadata prefer the actionable exception line over `Traceback (most recent call last):`
    - kept raw failure report artifacts and existing migrate backend env metadata intact
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
    - made interactive migrate rendering print per-target result lines in route order
    - included successful targets in the final migrate result block
    - deduplicated migrate `hint:` lines and printed one failure-log path block per failed target
    - printed migrate result blocks after successful interactive runs, not only on failure
  - `tests/python/actions/test_action_target_support.py`
    - added coverage for concise bounded migrate failure status emission
  - `tests/python/actions/test_action_spinner_integration.py`
    - added coverage for spinner updates from later status events and migrate interactive spinner suppression
  - `tests/python/actions/test_actions_parity.py`
    - added coverage for actionable migrate headline ordering and persisted migrate `headline` metadata
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`
    - added coverage for mixed-result migrate summaries, deduped hints, per-failure log paths, and all-success interactive migrate summaries

- Files/modules touched:
  - `python/envctl_engine/actions/action_target_support.py`
  - `python/envctl_engine/actions/action_command_orchestrator.py`
  - `python/envctl_engine/ui/dashboard/orchestrator.py`
  - `tests/python/actions/test_action_target_support.py`
  - `tests/python/actions/test_action_spinner_integration.py`
  - `tests/python/actions/test_actions_parity.py`
  - `tests/python/ui/test_dashboard_orchestrator_restart_selector.py`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_target_support` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_action_spinner_integration` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.actions.test_actions_parity` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_dashboard_orchestrator_restart_selector` -> passed
  - `PYTHONPATH=python python3 -m unittest tests.python.ui.test_terminal_ui_dashboard_loop` -> passed

- Config/env/migrations:
  - no config key changes
  - no migration revision changes
  - persisted migrate action metadata now includes an additive `headline` field for failures when available

- Risks/notes:
  - direct non-interactive `migrate` still prints the existing action-level success/failure lines outside the new dashboard result block; the concise per-target headline/report-path summary is fully implemented for dashboard-interactive rendering and persisted state consumers
  - older persisted migrate entries without `headline` fall back to reparsing `summary`, so no backfill or data migration is required
