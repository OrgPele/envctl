chore(prompts): tighten commit and PR handoff guidance

- Scope:
  - require `implement_task` to prefer `envctl commit --headless` before falling back to the git CLI
  - require `finalize_task` to finalize PR title and body/message to a high standard
  - apply a small prompt-formatting cleanup while keeping the shipped instructions unchanged
  - keep prompt-install regression coverage aligned with the updated prompt contracts

- Key behavior changes:
  - `python/envctl_engine/runtime/prompt_templates/implement_task.md`
    - changed the commit instruction so implementation flows prefer `envctl commit --headless`
    - kept the existing fallback path to plain git CLI when the envctl commit path fails
    - kept the final response contract aligned with reporting commit status
    - added a small markdown spacing cleanup without changing prompt semantics
  - `python/envctl_engine/runtime/prompt_templates/finalize_task.md`
    - clarified that PR open/update is not enough on its own
    - now requires the PR title and body/message to be detailed, accurate, polished, and finalized to a high standard
  - `tests/python/runtime/test_prompt_install_support.py`
    - updated prompt-template assertions so the shipped Codex prompt text is locked to the new commit and PR-quality instructions

- Files/modules touched:
  - `python/envctl_engine/runtime/prompt_templates/implement_task.md`
  - `python/envctl_engine/runtime/prompt_templates/finalize_task.md`
  - `tests/python/runtime/test_prompt_install_support.py`

- Tests run:
  - `PYTHONPATH=python python3 -m unittest tests.python.runtime.test_prompt_install_support` -> passed

- Config/env/migrations:
  - no config key changes
  - no env contract changes
  - no migrations

- Risks/notes:
  - this change updates prompt guidance only; it does not change envctl runtime execution behavior
  - agents still need the underlying `envctl commit --headless` path and PR tooling to succeed in their own worktrees for the guidance to be fully effective at execution time

### Envctl pointer ###
guidance only; it does not change envctl runtime execution behavior
  - agents still need the underlying `envctl commit --headless` path and PR tooling to succeed in their own worktrees for the guidance to be fully effective at execution time
