fix(runtime): honor explicit repo roots in CLI packaging

- Scope:
  - finalize the worktree by fixing the suite-level packaging/bootstrap regression
  - make explicit `--repo` resolution win over inherited `RUN_REPO_ROOT` in the direct runtime CLI
  - isolate packaging smoke tests from ambient envctl runtime env leakage

- Key behavior changes:
  - `python/envctl_engine/runtime/cli.py`
    - changes base-dir resolution so explicit `--repo` wins before inherited `RUN_REPO_ROOT`
    - rewrites `RUN_REPO_ROOT` to the explicit repo before route parsing and config loading so later bootstrap/prereq checks stay aligned
  - `tests/python/runtime/test_command_exit_codes.py`
    - adds a narrow unit regression proving `cli.run()` honors explicit `--repo` even when `RUN_REPO_ROOT` is already set
  - `tests/python/runtime/test_cli_packaging.py`
    - adds installed-command coverage for the same explicit repo precedence contract
    - isolates packaging/source-checkout test envs from inherited `RUN_REPO_ROOT`, `RUN_SH_RUNTIME_DIR`, `ENVCTL_ROOT_DIR`, `PYTHONPATH`, and related launcher/runtime variables so full-suite order pollution cannot change packaging outcomes

- Files/modules touched:
  - `python/envctl_engine/runtime/cli.py`
  - `tests/python/runtime/test_command_exit_codes.py`
  - `tests/python/runtime/test_cli_packaging.py`
  - `.envctl-commit-message.md`

- Tests run:
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes.CommandExitCodeTests.test_explicit_repo_arg_overrides_inherited_run_repo_root` -> pass
  - `python3 -m unittest tests.python.runtime.test_cli_packaging.CliPackagingTests.test_regular_install_without_dependencies_fails_before_operational_dispatch tests.python.runtime.test_cli_packaging.CliPackagingTests.test_regular_install_repo_arg_overrides_inherited_run_repo_root` -> pass
  - `python3 -m unittest tests.python.runtime.test_command_exit_codes tests.python.runtime.test_cli_packaging` -> pass (`Ran 61 tests`)
  - `envctl list-targets --main --json` -> pass (`Main` is the current project for this worktree root)
  - `envctl test --project Main` -> pass (`1669 passed, 0 failed, 0 skipped`, duration `1m 56s`)

- Config/env/migrations:
  - no config schema changes
  - no migrations
  - packaging tests now intentionally scrub inherited envctl runtime env so installed-command validation runs in an isolated environment

- Risks/notes:
  - the fix changes only the direct runtime CLI path (`envctl_engine.runtime.cli`); the repo wrapper/launcher already resolved explicit repo roots separately
  - the final validation target for this checkout is `Main`, based on `envctl list-targets --main --json`, not the branch slug
