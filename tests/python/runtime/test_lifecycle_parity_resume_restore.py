# Resume/restore lifecycle behavior is split across test_lifecycle_parity_resume_* and restore owner suites.
# Keep this breadcrumb so historical focused commands fail soft and point at the split suites.
