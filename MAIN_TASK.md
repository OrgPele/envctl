# Test headless tmux behavior
Create a minimal no-op implementation task for testing tmux headless launch behavior.
## Goal
Verify that envctl launches the AI session in tmux without attaching the current terminal when `--headless` is used.
## Constraints
- Do not modify unrelated files
- Prefer inspection over implementation
