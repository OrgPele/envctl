When the current implementation pass finishes, run the focused checks that fit the changes, then commit and push the branch. If this cycle needs `ship` and subagents are available, delegate `envctl ship --project <current-worktree-name> --json` to a background subagent and keep the main thread on non-overlapping work. Do not wait idly on PR checks during this intermediate cycle unless this is the final handoff.
