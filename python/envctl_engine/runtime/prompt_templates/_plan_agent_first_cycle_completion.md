When the current implementation pass finishes, leave a concise state note for the next queued cycle: what changed, which focused validation passed or still needs to run, and what remains against `MAIN_TASK.md`. Do not force commit/push/PR work here unless this cycle is explicitly at a handoff boundary. Keep `.envctl-commit-message.md` current so the later `envctl ship --project <current-worktree-name> --json` handoff, or its compact manual fallback, can commit the complete cumulative change.
