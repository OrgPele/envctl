When the current implementation pass finishes, run focused validation with `envctl test-focused --project <current-worktree-name>`, then delegate `envctl ship --project <current-worktree-name> --json` to a background subagent when that capability is available. The shipping subagent must report commit/push/PR failures, merge conflicts, failed checks, and actionable review comments immediately. Keep the main implementation thread moving on non-overlapping work while checks are pending, then before final handoff wait for GitHub status checks to complete and confirm all required checks pass.
