When the current implementation pass finishes, run focused validation with `envctl test-focused`, then delegate `envctl ship -m "<message>" --json` to a background subagent when that capability is available. Run bare `envctl ship` from inside the current worktree/project directory; add `--project <current-worktree-name>` only when operating from elsewhere. `ship` waits for GitHub checks until they pass, fail, time out, or report no check contexts and returns `operation_statuses`, `checks_state`, `passed_checks`, `failing_checks`, `pending_checks`, and `checks_error`. The shipping subagent must report commit/push/PR failures, merge conflicts, failed checks, pending-check timeout, no-checks-reported status, and actionable review comments immediately. Keep the main implementation thread moving on non-overlapping work while checks are pending, and only return to the shipping lane if the subagent reports an issue or final check completion.
