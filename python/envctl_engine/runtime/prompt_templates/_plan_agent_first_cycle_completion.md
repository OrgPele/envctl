When the current implementation pass finishes, run focused validation with `envctl test-focused`, then delegate `envctl ship --json` to a background subagent when that capability is available. The shipping subagent must report commit/push/PR failures, merge conflicts, failed checks, and actionable review comments immediately. Keep the main implementation thread moving on non-overlapping work while checks are pending, and only return to the shipping lane if the subagent reports an issue or final check completion. Before final handoff, wait for GitHub status checks to complete and confirm all required checks pass.
